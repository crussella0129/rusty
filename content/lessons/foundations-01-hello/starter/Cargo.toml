# Empty [workspace] table: this lesson project is its OWN workspace, so running
# `cargo` inside the copied sandbox does not attach to Rusty's outer workspace.
[workspace]

[package]
name = "hello"
version = "0.0.0"
edition = "2021"
